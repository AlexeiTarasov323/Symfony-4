{% extends 'base.html.twig' %}

{% block body %}
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Имя</th>
                <th scope="col">Фамилия</th>
                <th scope="col">Телефон</th>
                <th scope="col">Email</th>
                <th scope="col"></th>
             </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <th scope="row">
                    <div class="form-check">
                        <input class="form-check-input position-static"
                               type="checkbox" id="blankCheckbox" value="option1" aria-label="...">
                    </div>
                </th>
                    <td><mark>{{ client.name }}</mark></a></td>
                    <td><mark>{{ client.surname }}</mark></td>
                    <td><mark>{{ client.phone }}</mark></td>
                    <td><mark>{{ client.email }}</mark></td>
                <td><a href="{{ path('client_show', {'id': client.id}) }}" class="alert-link">Подробнее</a>
                    <span>/</span>
                    <a href="{{ path('client_delete', {'id': client.id}) }}" class="alert-link">Удалить</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<div class="row justify-content-md-center">
   <div class="navigation">
        {{ knp_pagination_render(clients) }}
    </div>
</div>
{% endblock %}